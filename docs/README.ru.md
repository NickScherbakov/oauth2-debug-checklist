# Чеклист отладки OAuth 2.0 🔍

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![PRs Welcome](https://img.shields.io/badge/PRs-welcome-brightgreen.svg)](http://makeapullrequest.com)
[![GitHub issues](https://img.shields.io/github/issues/NickScherbakov/oauth2-debug-checklist)](https://github.com/NickScherbakov/oauth2-debug-checklist/issues)
[![GitHub stars](https://img.shields.io/github/stars/NickScherbakov/oauth2-debug-checklist?style=social)](https://github.com/NickScherbakov/oauth2-debug-checklist)

Практический чеклист для отладки реализаций **OAuth 2.0 Authorization Code Flow**.
Сэкономьте часы отладки, систематически проверяя эти реальные проблемы и частые ошибки.

> 💡 **Совет**: Добавьте эту страницу в закладки! Когда OAuth не работает, проверьте каждый пункт ниже **перед** тем, как начинать случайную отладку.

🌍 **[English version](../README.md)** | **Русская версия**

---

## 🎯 Зачем это нужно

OAuth 2.0 широко используется для делегированной аутентификации (например, "Войти через Google", "Войти через GitHub").
Даже опытные разработчики часто сталкиваются с проблемами реализации, которые:

- ❌ не являются синтаксическими ошибками,
- ❌ не являются багами библиотек,
- ✅ **это концептуальные несоответствия и ошибки конфигурации**.

Этот чеклист собирает распространённые моменты, которые часто требуют внимания при отладке потоков авторизации OAuth 2.0 в реальном коде.

---

## ✅ Чеклист

Каждый пункт ниже сохранил часы времени отладки для разработчиков, решающих реальные проблемы.

### 1. **🔗 Несоответствие Redirect URI**
   
**Проблема**: Провайдер отклоняет запрос авторизации или callback завершается с ошибкой.

**Проверьте**:
- Убедитесь, что redirect URI **точно** совпадает с тем, что зарегистрировано у провайдера
- Регистр имеет значение: `http://localhost/callback` ≠ `http://localhost/Callback`
- Слеш в конце имеет значение: `/callback` ≠ `/callback/`
- Query параметры должны совпадать, если указаны
- Протокол должен совпадать: `http://` ≠ `https://`

**Пример ошибки**:
```
error: redirect_uri_mismatch
error_description: The redirect URI provided does not match a registered redirect URI
```

---

### 2. **🛡️ Отсутствующий / неправильный параметр state**

**Проблема**: Уязвимость CSRF или валидация callback не проходит.

**Проверьте**:
- Включите параметр `state` в запрос авторизации
- Генерируйте случайное, уникальное значение для каждого запроса
- Храните его безопасно (сессия/cookie)
- Проверяйте совпадение при обработке callback
- State должен возвращаться провайдером без изменений

**Почему это важно**: Без `state` злоумышленники могут выполнить CSRF-атаки.

---

### 3. **🔐 Client secret раскрыт во frontend**

**Проблема**: Уязвимость безопасности — секрет утекает в браузер/мобильное приложение.

**Проверьте**:
- Authorization Code Flow требует client secret **только на backend**
- Никогда не включайте `client_secret` в JavaScript, мобильные приложения или браузер
- Обмен токенов (code → access_token) должен происходить на стороне сервера
- Для SPA/мобильных приложений используйте PKCE вместо этого

**Красный флаг**: Если вы видите `client_secret` в frontend коде — **остановитесь и переделайте**.

---

### 4. **🎫 Путаница токенов**

**Проблема**: Отправка неправильного типа токена или путаница authorization code с access token.

**Различайте**:
- **Authorization code**: Кратковременный, одноразовый, обменивается на токены
- **Access token**: Используется для доступа к защищенным ресурсам
- **Refresh token**: Используется для получения новых access токенов
- **ID token** (OpenID Connect): Содержит утверждения о личности пользователя

**Частая ошибка**: Попытка использовать authorization code для вызова API (это не сработает).

---

### 5. **⚙️ Особенности конкретных провайдеров**

**Проблема**: Интеграция работает с одним провайдером, но не работает с другим.

**Проверьте**:
- Google требует `access_type=offline` для refresh токенов
- Microsoft может требовать `prompt=consent` для определённых scope
- GitHub имеет специфичные имена scope
- Некоторые провайдеры требуют параметр `response_mode`
- Всегда читайте **документацию конкретного провайдера**

**Ресурсы**:
- [Google OAuth 2.0](https://developers.google.com/identity/protocols/oauth2)
- [Microsoft identity platform](https://docs.microsoft.com/en-us/azure/active-directory/develop/)
- [GitHub OAuth](https://docs.github.com/en/apps/oauth-apps/building-oauth-apps)

---

### 6. **❌ Ошибки token endpoint**

**Проблема**: Обмен токенов завершается с загадочной ошибкой.

**Отладка**:
- Изучите ответы с ошибками от token endpoint провайдера
- Частые ошибки:
  - `invalid_grant`: Code уже использован, истёк или неверен
  - `invalid_client`: Неправильный client_id или client_secret
  - `unsupported_grant_type`: Неправильный параметр grant_type
- Логируйте полный ответ с ошибкой (но скрывайте секреты)
- Проверьте content-type запроса (обычно `application/x-www-form-urlencoded`)

---

### 7. **🕐 Расхождение часов (Clock skew)**

**Проблема**: Токены отклоняются как истёкшие, даже когда они кажутся валидными.

**Проверьте**:
- Убедитесь, что время сервера синхронизировано (используйте NTP)
- Утверждения токена `exp` (истечение) и `iat` (время выдачи) зависят от точного времени
- Некоторые провайдеры допускают небольшое расхождение часов (обычно 5 минут)
- Проверьте настройки часового пояса

**Быстрый тест**: `date` на Linux/Mac или `echo %date% %time%` на Windows

---

### 8. **🔒 Отсутствие PKCE (для SPA и мобильных приложений)**

**Проблема**: Сниженная безопасность для публичных клиентов (SPA, мобильные приложения).

**Проверьте**:
- **Всегда используйте PKCE** для SPA и мобильных приложений (RFC 7636)
- PKCE устраняет необходимость в client_secret для публичных клиентов
- Генерируйте `code_verifier` (случайная строка из 43-128 символов)
- Вычислите `code_challenge` = BASE64URL(SHA256(code_verifier))
- Отправьте `code_challenge` и `code_challenge_method=S256` в запросе авторизации
- Отправьте `code_verifier` в запросе обмена токенов

**Почему это важно**: Предотвращает атаки перехвата authorization code.

**Ресурсы**: [RFC 7636 - PKCE](https://datatracker.ietf.org/doc/html/rfc7636)

---

### 9. **🌐 Проблемы с CORS**

**Проблема**: Браузер блокирует OAuth-запросы с frontend.

**Проверьте**:
- Authorization endpoint redirect: CORS не применяется (навигация браузера)
- Token endpoint: Должен иметь правильные CORS заголовки, если вызывается из браузера
- **Лучшая практика**: Никогда не вызывайте token endpoint из браузера (используйте backend прокси)
- Если неизбежно, убедитесь, что провайдер разрешает ваш origin в политике CORS
- Проверьте консоль браузера на CORS ошибки

**Красный флаг**: Если вы вызываете token endpoint из JavaScript, пересмотрите вашу архитектуру.

---

### 10. **🎭 Проблемы со scope**

**Проблема**: Не можете получить доступ к ожидаемым ресурсам или отсутствуют разрешения.

**Проверьте**:
- Запрашивайте все необходимые scope в запросе авторизации
- Scope разделяются пробелами: `scope=openid profile email`
- Пользователи могут отклонить scope (проверьте предоставленные scope в ответе токена)
- Некоторые scope требуют одобрения провайдера или специальных разрешений
- Имена scope специфичны для провайдера (Google: `profile`, GitHub: `user`)

**Пример**:
```
# Запрашиваем
scope=openid profile email read:user

# Проверяем предоставленные scope
granted_scopes = token_response.get('scope')
```

---

### 11. **🔄 Проблемы с обновлением токенов**

**Проблема**: Невозможно получить новые access токены после истечения.

**Проверьте**:
- Запросите scope `offline_access` или эквивалент для refresh токенов
- Google требует параметр `access_type=offline`
- Refresh токены могут истечь или быть отозваны
- Храните refresh токены безопасно (зашифрованными в хранилище)
- Обрабатывайте ротацию refresh токенов (некоторые провайдеры выдают новый refresh токен)
- Проверьте, поддерживает ли провайдер refresh токены (некоторые требуют повторной аутентификации)

---

### 12. **🔍 Пропущена валидация ID Token**

**Проблема**: Уязвимость безопасности - принятие поддельных утверждений о личности.

**Проверьте** (для OpenID Connect):
- **Всегда проверяйте подпись ID токена** используя публичные ключи провайдера (JWKS)
- Проверьте, что `iss` (issuer) совпадает с вашим провайдером
- Проверьте, что `aud` (audience) совпадает с вашим client_id
- Проверьте, что `exp` (expiration) не прошёл
- Проверьте `nonce` если используется (предотвращает replay атаки)

**Никогда** не декодируйте ID токен без предварительной проверки подписи!

**Примеры библиотек**: 
- Node.js: `jsonwebtoken`, `jose`
- Python: `PyJWT`, `python-jose`

---

### 13. **⚡ Использование implicit flow (устарело)**

**Проблема**: Использование устаревшего, менее безопасного потока.

**Проверьте**:
- **Не используйте Implicit Flow** (`response_type=token`)
- Implicit Flow устарел и небезопасен
- Всегда используйте **Authorization Code Flow** с PKCE для SPA
- Authorization Code Flow держит токены вдали от истории браузера
- PKCE обеспечивает безопасность без client_secret

**Миграция**: Замените Implicit Flow на Authorization Code + PKCE.

---

## 💻 Примеры кода

Хотите увидеть эти лучшие практики в действии? Посмотрите наши рабочие примеры кода:

- **[Node.js / Express](../examples/nodejs/)** - Полная реализация с объяснениями
- **[Python / Flask](../examples/python/)** - Примеры OAuth 2.0 на Flask

Каждый пример демонстрирует:
✅ Все пункты чеклиста правильно реализованы
✅ Поддержка PKCE
✅ Правильная обработка ошибок
✅ Лучшие практики безопасности

---

## ❓ Часто задаваемые вопросы

### В: Нужен ли HTTPS для OAuth 2.0?

**О**: Да, для продакшена. Спецификация OAuth 2.0 требует HTTPS, кроме разработки на localhost. Использование HTTP в продакшене является уязвимостью безопасности.

### В: Могу ли я использовать один и тот же redirect URI для нескольких приложений?

**О**: Нет. Каждое OAuth-приложение должно иметь свой собственный зарегистрированный redirect URI для изоляции безопасности.

### В: Как долго живут access токены?

**О**: Зависит от провайдера:
- Google: 1 час
- GitHub: Без истечения (до отзыва)
- Microsoft: 1 час
- Всегда проверяйте поле `expires_in` в ответе токена.

### В: Стоит ли хранить токены в localStorage?

**О**: 
- ❌ **localStorage**: Уязвим для XSS-атак
- ✅ **httpOnly cookies**: Лучшая безопасность для веб-приложений
- ✅ **Безопасная backend сессия**: Лучший вариант
- Для мобильных: Используйте безопасное хранилище (Keychain/Keystore)

### В: В чём разница между OAuth 2.0 и OpenID Connect?

**О**: 
- **OAuth 2.0**: Фреймворк авторизации (что вы можете получить?)
- **OpenID Connect**: Слой идентификации поверх OAuth 2.0 (кто вы?)
- OIDC добавляет ID токен с утверждениями о личности пользователя
- Если вам нужна аутентификация (вход), используйте OpenID Connect

---

## 🤝 Внесение вклада

Нашли распространённый случай отладки OAuth 2.0, не охваченный здесь? **Будем рады услышать от вас!**

Пожалуйста, смотрите наши подробные [Руководство по внесению вклада](../CONTRIBUTING.md).

**Быстрые ссылки**:
- 🐛 [Сообщить о проблеме](https://github.com/NickScherbakov/oauth2-debug-checklist/issues/new/choose)
- 💡 [Предложить улучшение](https://github.com/NickScherbakov/oauth2-debug-checklist/issues/new?template=documentation-gap.yml)
- 🔧 [Открыть Pull Request](https://github.com/NickScherbakov/oauth2-debug-checklist/pulls)

---

## 📚 Дополнительные ресурсы

### 📖 Официальные спецификации
- [RFC 6749 - OAuth 2.0 Framework](https://datatracker.ietf.org/doc/html/rfc6749)
- [RFC 7636 - PKCE](https://datatracker.ietf.org/doc/html/rfc7636)
- [OAuth 2.0 Security Best Current Practice](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-security-topics)
- [OpenID Connect Core](https://openid.net/connect/)

### 🔧 Инструменты и библиотеки
- [jwt.io](https://jwt.io/) - JWT декодер и отладчик
- [OAuth Debugger](https://oauthdebugger.com/) - Тестирование OAuth потоков

---

## 📜 Лицензия

Этот репозиторий выпущен под [MIT License](../LICENSE).

---

## ⭐ Поддержка

Если этот чеклист сэкономил вам время, пожалуйста:
- ⭐ **Поставьте звезду** этому репозиторию - Помогает другим найти его!
- 🔗 **Поделитесь** им со своей командой и в социальных сетях
- 📝 **Внесите вклад** своими собственными идеями отладки
- 💬 **Распространяйте слово** - Твитните об этом, напишите об этом в блоге!

**Каждая звезда помогает большему количеству разработчиков избежать головной боли с отладкой OAuth!** 🚀

**Удачной отладки!** 🎉
